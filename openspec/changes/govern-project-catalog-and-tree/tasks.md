## 1. 项目主数据

- [x] 1.1 实现公共清单、访问策略与管理 API，回归合并/冲突/排除/恢复。
- [x] 1.2 接入目录扫描、业务路由、登记和资源消费者，验证直接路径守卫。
- [x] 1.3 项目库管理全部目录与排除状态，统一发现查询，验证错误恢复。

## 2. Git 层级

- [x] 2.1 树构建、单链压缩、折叠与状态展示，验证刷新和特殊路径。

## 3. 交付

- [x] 3.1 前后端专项、类型与构建、宿主与 Forge 门禁。
- [x] 3.2 桌面/窄屏验收、新版本运行与至少 60 秒稳定观察。
- [x] 3.3 同步文档、审查并提交本次改动。
- [ ] 3.4 通过 Forge 工具登记接口证据（本轮工具返回 user rejected MCP tool call，未登记，不绕过）。

## 已有验证证据

- 前端 typecheck、架构边界与完整 npm build 通过，29 项专项通过。
- 最终后端专项 54 项通过，包含真实 SQLite 登记/初始化事务、排除/恢复、路径守卫、路由冲突及空响应 HTTP 204 契约；相关模块 install 通过。
- 42 模块宿主 install 通过；运行采用开发模式，前端由 Vite 提供，未声称跳过前端复制的 JAR 包含本次静态资源。
- 真实组件在独立浏览器验收页检查，目录管理 API 使用测试响应；1440px 与 390px、折叠/展开、排除/恢复通过，无页面异常或横向溢出。首次发现空 200 响应无法解析，已修复为 204 并回归。
- 主要实现已由工作区保存提交 b6fe6aa7 记录；该提交明确为开发快照。当前继续完成响应契约、加载守卫与运行验收，不能把保存提交当交付通过。
- 本地原始记录位于 `.codex-work/catalog-*`，不提交日志、凭据或临时页面。

## 运行验收

- 新 JVM 83044，18080 开发后端与 5173 前端管理进程稳定观察 67.9 秒，进程身份与重启次数未变。
- 实际 catalog/workspaces/projects 路径及显示名称一致；临时目录排除、直接路径拒绝、恢复通过，测试目录已清理。
- Forge JSON status=PASSED，退出码 0；执行项见本地 catalog-quality.json，未执行项不宣称通过。启动日志核对 PID 与 Started，无致命启动错误。
- 界面验收是独立真实组件页面，API 使用测试响应；未声称已在已登录业务页面执行完整端到端浏览器验收。
- 接口证据登记工具调用被拒绝，任务 3.4 保留未完成；无人工待执行 SQL。


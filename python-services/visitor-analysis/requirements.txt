# FastAPI + uvicorn 提供 HTTP 服务壳（与 faster-whisper sidecar 同构）。
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# openai：调第三方 OpenAI 兼容平台（base_url 可指向任意兼容端点）。
openai>=1.30.0
# agentscope：模型层 + Studio 可观测性（token/cost/trace 自动上报）。
# Python 是 AgentScope 的旗舰 SDK，特性最全更新最快。
agentscope>=1.0.0

## 1. 边界与复现

- [x] 1.1 核验退役提交、缺失 WS 路由及宿主 500，确认用户保留胶囊、移除委托的目标。
- [x] 1.2 补充胶囊入口只读映射复现测试，记录修复前失败。

## 2. 独立胶囊接入

- [x] 2.1 实现独立胶囊配置、宿主认证、稳定身份和网关，保持现有 URL/配置兼容。
- [x] 2.2 覆盖无效身份、动态撤销、managed 空列表、命令限制、语音控制和会话隔离。
- [x] 2.3 同步胶囊保留与委托退役边界说明。

## 3. 交付验收

- [x] 3.1 运行专项测试、完整宿主装配和 OpenSpec 严格校验。
- [x] 3.2 通过 Forge 重启目标版本，验证真实胶囊握手与会话恢复、委托路由仍不存在。
- [x] 3.3 执行 Forge 全阶段门禁并观察至少 60 秒稳定运行。
- [x] 3.4 核对本次 diff 与明确路径的提交范围，准备自动提交。

## 验收证据（2026-09-15）

- 修复前只读入口测试失败；修复后 49 项专项测试全部通过。完整 42 模块宿主 package 成功，前端 typecheck/build 与 SDK 构建通过。
- 使用 node forge.mjs start 启动 dev 模式：后端 18080、前端 5173。后台主服务 PID 8896/32360，自 16:17 至 16:18 复查不变，重启次数 0；Studio PID 14040 正常。当前启动无致命启动异常。
- 恢复既有计划任务 Yoooni One Forge Reverse Tunnel，测试服务器 28080 转发本机 18080，HTTP 200。
- 测试站真实浏览器：16:18:09 握手成功并收到 ready；16:18:30 重连发送 attach，16:18:31 收到 ready。会话反馈记录正常显示。委托实现与目标 classes 仍无退役控制器。
- Forge 全阶段验证 exit 0、status PASSED；9 项 API 运行场景通过，executedCheckers 为空，未声称额外静态检查执行。
- 范围外已有异常：微信服务 waiting restart；本地 ASR 9500 不在线，未验收语音识别。

## 1. 服务端模型策略

- [x] 1.1 增加管理员默认模型的脱敏配置读写与权限边界，保存模型实际 ID 和展示名称。
- [x] 1.2 在业务咨询会话创建边界区分管理员与普通用户，并增加未配置、覆盖请求和管理员单次选择测试。

## 2. 前端可见性

- [x] 2.1 管理员从当前模型展示目录选择并保存默认，普通用户读取默认模型并只读使用。
- [x] 2.2 增加组件回归测试，证明普通用户只禁用模型而不禁用其它运行选项。

## 3. 验证与交付

- [x] 3.1 通过前后端专项测试、类型检查、OpenSpec strict validation 和适用质量门禁。
- [x] 3.2 核对差异并提交本次固定模型策略；不重启服务。

## 4. 统一业务咨询 Auth

- [x] 4.1 扩展单例策略保存服务端发现的 Auth 目录，并在新咨询创建边界忽略客户端覆盖。
- [x] 4.2 管理员可保存模型与 Auth；普通用户只读使用；未配置或未保存时提供明确恢复提示。
- [x] 4.3 完成后端、前端、OpenSpec、设计和质量门禁验证，并提交；不重启服务。

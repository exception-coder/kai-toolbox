## 1. 数据与后端

- [x] 1.1 新模块注册、幂等 DDL 与 Excel 种子，验证数量和来源。
- [x] 1.2 公告、站点、规则持久化 API 与输入校验。
- [x] 1.3 Patchright 采集、任务状态与失败恢复。
- [x] 1.4 确定性候选提取、统一 LLM 解析与证据校验。

## 2. 工作区

- [x] 2.1 manifest 注册，概览、结果、详情、站点与关键词管理。
- [x] 2.2 加载、空、错误与响应式状态。

## 3. 验证

- [x] 3.1 后端成功、失败、边界与数据库集成测试。
- [ ] 3.2 前端类型检查及交互测试。交互测试 3 项通过，全仓类型检查受既有 sessionClient.test.ts:66 两个错误阻塞。
- [x] 3.3 实采和桌面、移动端浏览器验证。
- [x] 3.4 Forge 质量门禁与 OpenSpec 严格校验，记录限制与工作日志。
- [ ] 3.5 统一网关恢复额度后验证真实模型成功解析。真实调用返回 insufficient_user_quota，已验证待核验降级和重试提示。

## 4. 验证证据

- 种子：8 个站点、171 条规则，60 行来源去重为 59 条公告；重复行保存在历史对照中。
- Maven：新模块 6 项集成测试通过；完整 starter 非 clean 打包通过（skip.frontend=true）。
- 前端：3 项规则编辑/恢复测试通过，feature-catalog 与架构边界通过；Vite 生产构建通过，未将其等同于 TypeScript 全仓通过。
- 采集器：3 项 Node 测试通过；真实首轮任务 dc2557da-3d66-45ae-a7ee-7eb3959bde2d 完成，成功 50、失败 9（河北 7、阳泉 2），结果存入宿主 SQLite。
- 浏览器：1440px 桌面和 390px 移动端模块工作区无页面错误、无页面横向溢出；规则编辑和筛选空态经过操作验证。正式 Forge 路由保留现有登录门禁。
- 运行接口：真实 HTTP 规则新增、修改、读取、删除验证通过，仅使用临时 QA 规则，结束已删除。
- Forge 门禁：status=PASSED，exit=0；executedCheckers 为空，实际 API-RUNTIME-001 检查 toolbox-api-tools、procurement-overview、procurement-rules，均 PASSED。
- 已通过监督器加载模块并复核 /api/procurement/overview 返回 59 条公告、50 条成功、9 条失败。模型额度不足，不声称解析成功或自动补充销售结论。

## 5. 销售判断结构扩展

- [x] 5.1 导入销售判断候选 47 列结构，版本化字段管理与并发校验。
- [x] 5.2 模型按配置字段提取，类型与原文校验，保存结构快照。
- [x] 5.3 公告结构展示、人工修正与恢复自动值，重解析保护。
- [x] 5.4 后端、前端、浏览器及运行门禁验证，更新工作日志。真实模型成功验收仍受 3.5 所列网关额度限制。

## 6. 全国平台当日链接发现

- [x] 6.1 验证来源与省份控件，固化 Patchright 查询、分页、日期与重复保护。
- [x] 6.2 持久化发现批次、逐页链接和查询完成状态，去重登记五省公告。
- [x] 6.3 新增链接工作区，分离链接发现、批次详情和批次解析。
- [x] 6.4 自动测试、真实当天发现、浏览器验证及工作日志。

### 结构扩展验证证据

- 模板与第二页逐列对应 47 字段，4 SYSTEM、32 LLM、11 MANUAL；未导入黄金样本值为新事实。
- 10 项 Java 集成测试通过，新增 4 项包含动态字段白名单、模拟模型成功及提示/版本快照、金额单位和日期校验、多个候选不取首值、人工覆盖及并发冲突。模拟成功不等同真实网关成功。
- 前端 7 项交互测试通过。Vite 生产构建通过；全仓类型检查仍仅报告 sessionClient.test.ts:66 两个既有错误，FeatureManifest 目录和架构边界通过。
- 实际 Chrome Forge 页面验证字段说明保存（结构到 v3）、公告分组、临时人工备注保存与恢复；真实 API 确认 47 字段且临时覆盖已清空。桌面与 390px 响应式渲染通过，临时视口已恢复。
- Forge gate exit=0、status=PASSED，executedCheckers=[]；API-RUNTIME-001 实际执行原三个场景及新增 procurement-structure 四个 HTTP 场景通过。

### 当日链接验证证据

- 2026-09-06 实际 DOM 核实六来源；省平台具备五省筛选，其他来源未显示省份控件，读取列表省份作为依据，未知地区不自动进入正文队列。
- 正式宿主批次 d530eca4-9f0d-4219-b21e-26e0e7499434：COMPLETED，10 组查询全部完成，19 页；央企 3 页、财政部 8 页，其他查询各 1 页，零结果有明确完成记录。URL 去重后五省 41 条、地区待确认 59 条。
- 宿主公告从 59 增至 100，新增 41 条均 PENDING；历史采集成功 50、失败 9 保持不变，本轮未请求详情或付费 LLM。
- 初次失败批次 95d3bc06-7ea1-4b72-8e2e-14c50709274d 保留：Maven starter 工作目录导致脚本不可见。路径解析修复后完成真实重跑，补 starter/root/绝对目录边界测试。
- Java 14 项、前端 9 项、Node 6 项通过。Vite 构建通过；全仓类型检查仍受既有 sessionClient.test.ts:66 两个错误限制。
- 实际 Forge 按钮触发、运行进度、地区待确认分页（59 条分两页）、1440px/390px 布局验证通过，视口已恢复。Forge gate status=PASSED、exit=0，五项 API-RUNTIME-001 场景通过，executedCheckers=[]。

- [x] 当日链接接入采集站点选择；未适配站点不显示全国批次、不允许启动采集；10 项前端测试通过。全量类型检查仍阻于既有 sessionClient.test.ts:66。

## 7. 缓存与业务解析

- [x] 7.1 HTML/iframe 缓存持久化与不重复访问验证。
- [x] 7.2 明确标签提取与 Codex 未解决字段补充、原文校验及运行留痕。
- [x] 7.3 结果按站点和业务列展示、人工触发、缓存下载。
- [x] 7.4 现有链接实采、真实 Codex 验证、自动测试和页面验收。

### 缓存与业务解析验证证据

- 当日 41 条五省链接：40 条完整保存渲染 HTML、允许域 iframe HTML 和正文，共 2,419,774 字符；1 条正文 iframe 两次未就绪，保持失败。缓存不包含外部图片、样式文件或附件文件。
- 缓存批次 f53379d8-01bc-4c02-b7da-0b4f7e51faee：40/40 完成，仅解析本地正文；已有模型结果保留。前后抽查 29 条已有采集时间完全一致。失败且无正文的一条不进入本地解析队列。
- 三份真实缓存分别验证施工招标、合同、变更公告的 Codex 解析。合同最终复核批次 97176fac-3e93-49a7-900b-7eba07a3a384 成功；源站项目编号填入项目名称时留空，金额缺少单位时留空。施工金额 102730439.82 元经代码换算为 10273.043982 万元。其余 37 条保留代码提取结果，待人工触发模型解析。
- 固定规则处理明确标签与唯一值，角色边界不清时不分配联系人电话；Codex 仅补未解决字段，输出经字段白名单、类型、原文证据校验。失败保留此前有效结果，人工修正独立保存。
- 当前结果只返回发现链接对应公告，按站点筛选并直接展示 19 个启用业务列；历史 Excel 公告仍保留数据库中，不混入当前列表。评测定义与解析记录不进入业务列表载荷。Chrome 实际页面确认平台选择、19 列、缓存/解析按钮和 41 条分页，本轮未另做移动端截图验收。
- 18 项 Java、11 项前端、7 项 Node 测试通过，Vite 构建通过；全仓类型检查仍仅报告既有 sessionClient.test.ts:66 两项错误。Forge gate exit=0、status=PASSED，六个 API-RUNTIME-001 场景实际执行通过，executedCheckers=[]，不能视为静态检查器验收。

## 8. 字段级校验与重试诊断

- [x] 8.1 实现明确标签单位、证据空白对齐与逐字段保留。
- [x] 8.2 实现带校验反馈的两轮上限重试和诊断登记。
- [x] 8.3 详情诊断入口、自动回归及 16 条缓存实际重试验收。

### 字段重试验证证据

- 16 条历史失败缓存实跑：15 条 PARSED；天津澄清公告经两轮保留三项合法事实并标记 PARTIAL，完整登记原始输出及金额格式拒绝原因。据此补入 UNIQUE_EXPLICIT_AMOUNT_SPAN 规则，最终批次 38f35573-7204-4add-91d0-2d96279b7029 成功，99,775,461 元换算为 9977.5461 万元。
- 最终全部 40 条有正文公告 PARSED，另 1 条缺正文仍 PENDING；16 条采集时间未变。PARSED 仅表示执行与输出校验通过，不代表字段齐全或人工评测准确率。
- 新增诊断页签及最近 50 次记录接口，业务列表保持 19 列；历史失败记录保留，当前记录附规则版本、字段状态及原始模型输出。Chrome 页面确认已修复金额显示；诊断页签交互由组件测试验证，未另做移动端截图。
- 25 项 Java 测试、12 项前端测试通过，Vite 构建通过；全仓类型检查仍受既有 sessionClient.test.ts:66 两项错误限制。Forge gate exit=0、status=PASSED，六项 API 检查实际通过，executedCheckers=[]。

## 10. 业务结果 Excel 导出

- [x] 10.1 按当前搜索、站点、采集状态导出全部匹配公告；复用业务投影与人工修正，保留启用字段顺序。
- [x] 10.2 提供真实 XLSX 下载、冻结表头及筛选；文本不作为公式执行，空值保持为空。
- [x] 10.3 验证跨页导出、字段类型及前端入口。

## 9. 解析经验工作区

- [x] 9.1 登记已生效规则、正反例和实现测试来源。
- [x] 9.2 复用生产校验执行离线回归并持久化结果。
- [x] 9.3 工作区展示、搜索、人工触发回归及验证。

### 解析经验验证证据

- experience-v1 登记 9 条规则、18 个正反例；其中 14 个字段样例可离线运行，另外 4 个流程正反例明确标注为流程单测覆盖。来源区分真实失败格式与构造边界样例，不冒充黄金样本。
- 回归使用生产 ProcurementFieldReview、证据白名单、类型转换和运行时字段配置，固定 schema/词典快照；独立 procurement_example_run 表保留预期、实际、版本和诊断，不修改公告且不调用模型。
- 本机记录 7f4fef15-0687-49b4-b09c-23451aec6fb0：14/14 符合预期，field-review-v2，schema v5；GET 历史记录和 Chrome 真实工作区均可查看。独立无会话浏览器等待页面超时，改用现有 Chrome 会话确认导航、目录和记录；未声称移动端截图通过。
- 26 后端、13 前端测试通过，Vite 构建通过。Forge gate exit=0、status=PASSED，8 项 API 场景实际通过，executedCheckers=[]。全仓类型检查仍有 sessionClient.test.ts:66 两项既有错误。

导出验证：真实 API XLSX 为 41 行、19 列；FAILED 筛选 1 行。Excel 单测及 13 前端测试通过，Vite 构建通过；Forge gate status=PASSED、exit=0（8 项运行场景，未执行静态 checker）。
## 11. 合并关键词规则

- [x] 11.1 按判定目的生成分组，原条目保留追溯，分组可保存和停用。
- [x] 11.2 解析快照使用合并规则，每轮只注入目标字段相关组和字典。
- [x] 11.3 分组工作区及规则选择回归、实际接口验证。

合并规则验证：数据库 11 个 GROUP 配置和 171 个原始条目；Chrome 实际页面显示字段解析 6、行业筛选 3、分类字典 2，原条目可追溯。29 后端测试、13 前端测试、Vite 构建通过；Forge gate exit=0/status=PASSED，9 项接口场景执行，executedCheckers=[]。OpenSpec strict 校验通过。全仓 tsc 仍有 sessionClient.test.ts:66 两处既有错误。未批量重解析公告，未声明模型准确率提升。

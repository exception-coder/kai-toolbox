请优化以下「工作经历」条目，使其更贴合目标岗位。

目标岗位：{{targetRole}}
工作年限：{{experienceYears}}
岗位级别：{{seniorityLevel}}

其他段摘要（仅供跨段一致性参考，不要改写其中内容）：
{{otherSectionsBrief}}

待优化的工作经历（JSON）：
{{originalContent}}

【本段输出要求】
- "optimizedContent" 必须是一个 JSON 字符串（注意：是被转义后的字符串，不是嵌套对象），其反序列化后的结构需与输入一致，包含字段：company、role、period、responsibilities（字符串数组）、achievements（字符串数组）。
- company / role / period 一般保持事实不变，仅在表述明显不规范时做轻微规整。
- responsibilities 与 achievements 重点优化：动词开头、结果导向、突出与目标岗位匹配的技术栈与业绩。
- 不得新增原文没有的项目或数字。

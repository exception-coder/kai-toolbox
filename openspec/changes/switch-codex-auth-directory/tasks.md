## 1. Implementation

- [x] 1.1 提供受限的 Codex Auth 目录发现接口。
- [x] 1.2 在当前官方 Codex 会话配置中展示可用目录。
- [x] 1.3 选择其他目录时确认并通过复制会话完成切换，保留源会话。
- [x] 1.4 将当前已加载对话显式交接给目标 Auth 的新 thread，并在目录发现失败时使用历史绑定目录降级。
- [x] 1.5 将交接固化为版本化结构包，按 OpenSpec → 文档/领域与 Graphify → 源码/Git/验证证据恢复，并显式标注规格缺口。
- [x] 1.6 持久化逻辑会话链路与 Auth 映射；首次创建并交接，后续往返直接恢复，唯一约束收敛重复请求。
- [x] 1.7 最近会话按规范化工作目录分组展示，不改变项目与会话身份。

## 2. Verification

- [x] 2.1 覆盖目录过滤、排序和前端确认行为。
- [x] 2.2 运行前端类型检查、专项测试、后端专项测试、OpenSpec 校验与 Forge 质量门禁。
- [x] 2.3 重新运行结构交接专项测试、前端构建、OpenSpec 严格校验与 Forge 质量门禁。
- [x] 2.4 覆盖 Auth 链路幂等映射并重新运行前后端及质量门禁。
- [x] 2.5 覆盖工作目录规范化、分组排序与缺失目录降级，并重新运行前端门禁。

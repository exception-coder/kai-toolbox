package com.exceptioncoder.forge.quality.runtime;

/** Status produced by runtime verification. */
public enum RuntimeStatus {
    /** The runtime assertion was satisfied. */
    PASSED,
    /** Execution or assertion failed. */
    FAILED
}

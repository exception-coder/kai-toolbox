## 1. Implementation

- [x] 1.1 合并工作台、页签与旧路由，保留配置和权限。
- [x] 1.2 内置新增模块能力与扩展注册契约。

## 2. Verification

- [x] 2.1 完成专项测试、类型检查、构建和质量门禁。
- [x] 2.2 完成浏览器验收和稳定观察。

## 验证证据

- 2026-09-13：14 项 Vitest 专项通过；typecheck、前端完整 build 通过，架构边界检查无新增违规。
- Forge CLI 全量门禁 status=PASSED、exit=0；executedCheckers 为空，不将其记为执行过静态检查；9 个 API-RUNTIME-001 场景实际通过。
- 浏览器核对五个系统页签、旧 ERP URL 参数与 hash、新增入口及返回焦点；390px 窄屏页面 scrollWidth=390，无横向溢出。未触发业务服务启动、配置保存或实际脚手架生成。
- dev 模式：前端 5173、后端 18080、Studio 3000。后端重建期间一次门禁连接失败，待新 JVM 18228 就绪后重验通过。三个采样对 9 个相关只读接口检查均为 HTTP 200；连续 113.21 秒进程及重启计数稳定，当段日志无致命异常，源码指纹保持一致。
- 微信服务仍为此前的 waiting restart（4 次），不属于本次前端交付范围。构建保留现有大 chunk 提示。

# SQL 候选

| 候选 | 目的 | 风险 | 验证前置 |
|---|---|---|---|
| 按 `prd_session` 旧路径回填首版账本 | 让存量产物进入统一事实账本 | 路径缺失、重复内容、历史版本不可还原 | 先生成只读盘点报告并核对摘要 |
| 为最新 `READY` 产物增加覆盖索引 | 支撑后续 API 切读 | 过早增加无收益索引 | 先以真实查询计划验证热点 |


## 1. Unified entry

- [x] 1.1 合并项目库导航与添加项目流程，兼容旧模块链接。
- [x] 1.2 项目详情默认打开 AI 工作区，保留原标签深链。

## 2. Scoped workspace

- [x] 2.1 模块工作区绑定当前项目，取消重复选择及错误 fallback。
- [x] 2.2 根目录对话、历史会话恢复及目录错误恢复入口。

## 3. Verification and delivery

- [x] 3.1 通过导航、路径隔离、会话恢复及未初始化场景测试。
- [x] 3.2 通过前端类型检查、构建、适用质量门禁及运行验收。
- [x] 3.3 更新说明，核对范围并自动提交。

## 4. Unified directories

- [x] 4.1 统一目录编辑及旧配置导入，首次保存使用单一请求切换来源。
- [x] 4.2 共用目录源，统一项目列表、扫描规则和本地操作授权。
- [x] 4.3 验证多根扫描、旧配置兼容、显式清空和根外拒绝，完成后端运行验收。

## 5. Session display names

- [x] 5.1 共用聊天会话显示名称，优先已有标题，缺失时显示目录名称；保留原会话 ID，不写入或生成新标题。
- [x] 5.2 验证命名与目录兜底、项目边界及原 ID 恢复，执行前端检查并记录验收边界。

2026-09-13 名称修正验证：项目库 16 个文件/65 项测试通过；纠正测试的跨模块导入后，ProjectAIWorkspace 最终 5 项复验通过，覆盖已有标题、空标题、空白标题、Windows/Unix 尾部分隔符及按原 sessionId 恢复。typecheck、完整前端构建、特性边界检查通过。Forge CLI 两次返回 PASSED，实际为 0 个静态 checker 与 9 个现有 API 运行检查，不把它们称为名称展示验收。构建期间前后端 PID 76188/80280、重启次数 0 和 ready 状态跨越 60 秒保持稳定；没有请求服务重启。名称展示由组件回归验证，实际浏览器仍受已保存的拒绝访问设置限制；此修正不改变布局、数据库标题或服务端接口。原语音及顶部模式切换修改不属于本次名称修正提交。

# Keep this file ASCII-only. pip on zh-CN Windows decodes requirements.txt with the
# system ANSI codepage (GBK/cp936); UTF-8 CJK bytes raise UnicodeDecodeError. Put any
# Chinese explanation in README.md, not here.

# FastAPI + uvicorn HTTP shell (same shape as the visitor-analysis / faster-whisper sidecars).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# WeChat automation library: pick ONE matching your WeChat desktop version.
# WeChat 3.9.x (legacy, most mature): keep the line below.
wxauto>=3.9.11.17
#
# WeChat 4.0+ (new): comment out wxauto above and use one of these instead
# (use whatever version each repo currently publishes):
# wxauto-4.0
# wxautox4
#
# server.py imports defensively (wxauto first, then wxautox); switching libs usually
# needs no code change. If a call fails, check /health (lib / lib_version) and adjust
# the method names in server.py against that library's docs.

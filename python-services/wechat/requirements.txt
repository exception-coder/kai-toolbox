# Keep this file ASCII-only. pip on zh-CN Windows decodes requirements.txt with the
# system ANSI codepage (GBK/cp936); UTF-8 CJK bytes raise UnicodeDecodeError. Put any
# Chinese explanation in README.md, not here.

# FastAPI + uvicorn HTTP shell (same shape as the visitor-analysis / faster-whisper sidecars).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# WeChat automation.
# NOTE: the open-source 'wxauto' is no longer published on PyPI. The maintained packages are
# wxautox (legacy WeChat 3.9.x) and wxautox4 (WeChat/Weixin 4.x). These are the commercial
# successor and MAY REQUIRE AN ACTIVATION LICENSE for some/all features.
# See https://pypi.org/project/wxautox4/ .
#
# This machine runs Weixin 4.x, so use wxautox4. wxautox4 transitively pulls comtypes /
# pywin32 / pillow / sounddevice etc. (all have cp313 wheels).
wxautox4
#
# For a legacy WeChat 3.9.x client instead, comment wxautox4 above and use:
# wxautox
